#version 150

in vec2 texCoord;
in uniform sampler2D texture;
vec4 fragColor;

void main()
{
    vec2 uv = gl_FragCoord.xy/iResolution.xy;
    fragColor = texture2D(texture, texCoord.st);
    gl_FragColor = fragColor;
}
